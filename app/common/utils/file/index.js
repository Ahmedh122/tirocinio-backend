/**
 * @fileoverview Welcome to hell, enjoy your stay.
 */

const { mergeObjects } = require('./mergeObjects');
const { getFileField, extractPathFromFileType } = require('./modelUtils');

module.exports = {
  mergeObjects,
  getFileField,
  extractPathFromFileType,
};
